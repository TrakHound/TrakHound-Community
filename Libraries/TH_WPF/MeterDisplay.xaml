<UserControl x:Class="TH_WPF.MeterDisplay"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             
             xmlns:levelindicator="clr-namespace:TH_WPF.LevelIndicator"
             
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">

    <Grid x:Name="root" HorizontalAlignment="Center">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="85"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="{Binding ValueText}" Foreground="{StaticResource Foreground_Normal}" FontSize="{Binding FontSize}" FontFamily="/TH_WPF;component/Fonts/#DS-Digital" Margin="10,0" HorizontalAlignment="Center" VerticalAlignment="Center"/>

        <levelindicator:Indicator x:Name="indicator" Grid.Column="1" Width="75" Margin="5" ActiveLevelBrush="{StaticResource Accent_Normal}" InactiveLevelBrush="{StaticResource Accent_Hover}"/>

    </Grid>

</UserControl>
