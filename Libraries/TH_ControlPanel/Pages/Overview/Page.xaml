<UserControl x:Class="TH_DeviceManager.Pages.Overview.Page"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="800">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/TH_Styles;component/All.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Background="#eee">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Device Overview" FontSize="20pt" Foreground="#333" Margin="0,0,0,10"/>

        <!--<TextBlock Grid.Row="1" Text="General Device Desciption Settings" FontSize="10pt" Foreground="#888" Margin="0,10,10,20"/>-->

        <Grid Grid.Row="2">

            <StackPanel>

                <TextBlock Text="Select whether this device is hosted on the TrakHound Cloud Server or a Local server" FontSize="10pt" Foreground="#888" Margin="0,10,10,20"/>

                <RadioButton x:Name="cloud_RADIO" Margin="10" Content="TrakHound Cloud" GroupName="Cloud" Checked="Cloud_Checked" Style="{StaticResource TH_RadioButton_Style}"/>

                <RadioButton x:Name="local_RADIO" Margin="10" Content="Local" GroupName="Cloud" Checked="Local_Checked" Style="{StaticResource TH_RadioButton_Style}"/>
                
            </StackPanel>
            
        </Grid>
        
    </Grid>

</UserControl>
