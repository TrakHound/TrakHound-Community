<UserControl x:Class="TH_DeviceManager.Pages.Databases.Page"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             
             xmlns:th_wpf="clr-namespace:TH_WPF;assembly=TH_WPF"
             
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    
    <Grid Background="#eee">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Databases" FontSize="20pt" Foreground="#333" Margin="10,0,0,10"/>

        <TextBlock Grid.Row="1" Text="Configure the Databases that this device writes data to reads from" FontSize="10pt" Foreground="#888" Margin="10,10,10,20"/>


        <StackPanel Grid.Row="2" Margin="10">

            <!--Add Database-->
            <StackPanel>

                <TextBlock Text="Add" FontSize="12pt" Foreground="#888" VerticalAlignment="Center"/>

                <ItemsControl ItemsSource="{Binding DatabaseTypeList}" Margin="0,10">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.Resources>
                        <Style TargetType="th_wpf:Button_01">
                            <Setter Property="MinWidth" Value="100"/>
                            <Setter Property="Height" Value="35"/>
                            <Setter Property="Margin" Value="0,0,10,0"/>
                            <Setter Property="HorizontalAlignment" Value="Left"/>
                            <Setter Property="AlternateStyle" Value="true"/>
                            <Setter Property="Image" Value="/TH_DeviceManager;component/Resources/Add_01.png"/>
                        </Style>
                    </ItemsControl.Resources>
                </ItemsControl>

            </StackPanel>

            <!--Configure Databases-->
            <StackPanel Margin="0,40,0,0">

                <TextBlock Text="List" FontSize="12pt" Foreground="#888" VerticalAlignment="Center"/>

                <ItemsControl ItemsSource="{Binding DatabaseList}" Margin="0,10" HorizontalContentAlignment="Stretch">
                    <ItemsControl.Resources>
                        <Style TargetType="th_wpf:CollapseButton">
                            <Setter Property="Margin" Value="0,10"/>
                        </Style>
                    </ItemsControl.Resources>
                </ItemsControl>

            </StackPanel>

        </StackPanel>

    </Grid>
    
</UserControl>
