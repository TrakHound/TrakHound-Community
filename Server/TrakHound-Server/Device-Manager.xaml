<Window x:Class="TrakHound_Server.Device_Manager"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        
        xmlns:user="clr-namespace:TH_UserManagement;assembly=TH_UserManagement"
        
        xmlns:th_wpf="clr-namespace:TH_WPF;assembly=TH_WPF"
        xmlns:loadinganimation="clr-namespace:TH_WPF.LoadingAnimation;assembly=TH_WPF"
                
        Title="TrakHound Server - Device Manager" 
        Icon="Resources/TrakHound_Logo_Initials_10.ico"
        
        Height="750"
        Width="1200"
        
        MinHeight="400"
        MinWidth="300"
        
        WindowStartupLocation="CenterScreen"
        WindowState="Maximized" 
        
        Closing="Window_Closing"
        
        FontFamily="arial"
                
        x:Name="Main_Window" 
        
        >

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/TH_Styles;component/All.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>


    <!--Window Content-->
    <Grid Height="auto" Width="auto" Background="{StaticResource Window_Background}">

        <DockPanel x:Name="Root_DOCK">

            <Border CornerRadius="0" Background="{StaticResource Window_Background}">

                <Grid>

                    <ContentControl Content="{Binding CurrentPage}"/>

                    <!--Temp Page-->
                    <Grid Background="{StaticResource Window_Background}">
                        <Grid.Style>
                            <Style TargetType="Grid">
                                <Setter Property="Visibility" Value="Visible"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding TempPage}" Value="{x:Null}">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Grid.Style>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <StackPanel Orientation="Horizontal">

                            <!--Back to Device Manager-->
                            <th_wpf:Button_04 Image="Resources/Back_01.png" Text="Back to Device Manager" Clicked="Back_Clicked"/>

                        </StackPanel>

                        <Grid Grid.Row="1" Margin="40,0,0,0">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <Grid Margin="0,20">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="auto"/>
                                </Grid.RowDefinitions>

                                <TextBlock Text="{Binding TempPage.PageName}" Foreground="#333" FontSize="30" Margin="0,10"/>

                                <Rectangle Grid.Row="1" Fill="#aaa" Height="0.5" Margin="0,5"/>

                            </Grid>

                            <ContentControl Margin="0,40,0,0" Grid.Row="1" Content="{Binding TempPage}"/>

                        </Grid>

                    </Grid>

                </Grid>

            </Border>

        </DockPanel>

    </Grid>

</Window>
