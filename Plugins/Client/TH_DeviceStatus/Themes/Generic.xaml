<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:TH_StatusTable.Controls"
    
    xmlns:root="clr-namespace:TH_StatusTable"
    xmlns:th_wpf="clr-namespace:TH_WPF;assembly=TH_WPF"
    >

    <ResourceDictionary.MergedDictionaries>

        <ResourceDictionary Source="/TH_Styles;component/All.xaml" />

    </ResourceDictionary.MergedDictionaries>

    <!--<ImageBrush x:Key="Power_IMG" Stretch="Uniform" ImageSource="/TH_StatusTable;component/Resources/Power_01.png"  />

    <ImageBrush x:Key="Alert_IMG" Stretch="Uniform" ImageSource="/TH_StatusTable;component/Resources/Warning_01.png"  />-->

    <!--<Style TargetType="{x:Type root:DeviceInfo}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type root:DeviceInfo}">

                    <Grid Height="50" Width="500" Background="#33000000">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="30"/>
                            <ColumnDefinition Width="200"/>
                            <ColumnDefinition Width="auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        --><!--Device Status--><!--
                        <Border>
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{TemplateBinding Production}" Value="true">
                                            <Setter Property="Background" Value="{StaticResource StatusGreen}"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{TemplateBinding Idle}" Value="true">
                                            <Setter Property="Background" Value="{StaticResource StatusYellow}"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{TemplateBinding Alert}" Value="true">
                                            <Setter Property="Background" Value="{StaticResource StatusRed}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>

                            --><!--Status Image--><!--
                            <Rectangle HorizontalAlignment="Center" VerticalAlignment="Center" Fill="White">
                                <Rectangle.Style>
                                    <Style TargetType="Rectangle">
                                        <Setter Property="Height" Value="20"/>
                                        <Setter Property="Width" Value="{Binding ActualHeight, RelativeSource={RelativeSource Self}}"/>
                                        <Setter Property="Margin" Value="0"/>
                                        <Setter Property="OpacityMask" Value="{StaticResource Power_IMG}"/>
                                        <Setter Property="Opacity" Value="0"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{TemplateBinding Alert}" Value="true">
                                                <DataTrigger.EnterActions>
                                                    <BeginStoryboard>
                                                        <Storyboard>
                                                            <DoubleAnimation Duration="00:00:1.0" Storyboard.TargetProperty="Opacity" From="0" To="1" >
                                                                <DoubleAnimation.EasingFunction>
                                                                    <CubicEase EasingMode="EaseOut"/>
                                                                </DoubleAnimation.EasingFunction>
                                                            </DoubleAnimation>
                                                        </Storyboard>
                                                    </BeginStoryboard>
                                                </DataTrigger.EnterActions>
                                                <DataTrigger.ExitActions>
                                                    <BeginStoryboard>
                                                        <Storyboard>
                                                            <DoubleAnimation Duration="00:00:1.0" Storyboard.TargetProperty="Opacity" From="1" To="0" >
                                                                <DoubleAnimation.EasingFunction>
                                                                    <CubicEase EasingMode="EaseOut"/>
                                                                </DoubleAnimation.EasingFunction>
                                                            </DoubleAnimation>
                                                        </Storyboard>
                                                    </BeginStoryboard>
                                                </DataTrigger.ExitActions>
                                            </DataTrigger>
                                            <DataTrigger Binding="{TemplateBinding Available}" Value="true">
                                                <Setter Property="OpacityMask" Value="{StaticResource Alert_IMG}"/>
                                            </DataTrigger>
                                            <Trigger Property="Opacity" Value="0">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Rectangle.Style>
                            </Rectangle>

                        </Border>

                        --><!--Description--><!--
                        <Grid Grid.Column="1" Background="Transparent">
                            --><!--<Grid.ToolTip>
                                <StackPanel Margin="10" Width="300">
                                    <StackPanel.Resources>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="FontSize" Value="12"/>
                                            <Setter Property="Margin" Value="0,2"/>
                                            <Setter Property="HorizontalAlignment" Value="Left"/>
                                        </Style>
                                    </StackPanel.Resources>

                                    --><!--Manufacturer Logo--><!--
                                    <th_wpf:ImageDisplay Image="{Binding ManufacturerLogo}" Padding="5" MaxHeight="60" MaxWidth="150" HorizontalAlignment="Left"/>

                                    <TextBlock Text="{Binding Description.Description}" Foreground="{StaticResource Foreground_Normal}" FontSize="14" TextWrapping="Wrap"/>

                                    <TextBlock>
                                                <Run Text="Manufacturer :" Foreground="{StaticResource Foreground_Light}"/>
                                                <Run Text="{Binding Description.Manufacturer}" Foreground="{StaticResource Foreground_Normal}"/>
                                    </TextBlock>

                                    <TextBlock>
                                                <Run Text="Model :" Foreground="{StaticResource Foreground_Light}"/>
                                                <Run Text="{Binding Description.Model}" Foreground="{StaticResource Foreground_Normal}"/>
                                    </TextBlock>

                                    <TextBlock>
                                                <Run Text="Serial :" Foreground="{StaticResource Foreground_Light}"/>
                                                <Run Text="{Binding Description.Serial}" Foreground="{StaticResource Foreground_Normal}"/>
                                    </TextBlock>

                                    <TextBlock>
                                                <Run Text="Controller :" Foreground="{StaticResource Foreground_Light}"/>
                                                <Run Text="{Binding Description.Controller}" Foreground="{StaticResource Foreground_Normal}"/>
                                    </TextBlock>


                                    <TextBlock>
                                                <Run Text="Location :" Foreground="{StaticResource Foreground_Light}"/>
                                                <Run Text="{Binding Description.Location}" Foreground="{StaticResource Foreground_Normal}"/>
                                    </TextBlock>

                                </StackPanel>
                            </Grid.ToolTip>--><!--
                            <Grid.RowDefinitions>
                                <RowDefinition>
                                    <RowDefinition.Style>
                                        <Style TargetType="RowDefinition">
                                            <Setter Property="Height" Value="6*"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding ManufacturerLogo}" Value="{x:Null}">
                                                    <Setter Property="Height" Value="0"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </RowDefinition.Style>
                                </RowDefinition>
                                <RowDefinition Height="4*"/>
                            </Grid.RowDefinitions>

                            --><!--Manufacturer Logo-->
                            <!--<th_wpf:ImageDisplay Image="{Binding ManufacturerLogo}" Padding="10,5" MaxHeight="40"/>-->


                            <!--Device Description-->
                            <!--<TextBlock Grid.Row="1" Text="{Binding DeviceDescription}" Foreground="{StaticResource Foreground_Normal}" FontSize="12" Margin="0,2" TextAlignment="Center" VerticalAlignment="Center" HorizontalAlignment="Center"/>--><!--

                        </Grid>
                        
                    </Grid>

                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>-->



    <Style TargetType="{x:Type local:BetterCell}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:BetterCell}">
                    <Canvas>

                        <!--Top Bar-->
                        <Rectangle Height="14" Width="{TemplateBinding ActualWidth}">
                            <Rectangle.ToolTip>
                                <Grid MinWidth="250">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="auto"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>

                                    <!--Title-->
                                    <TextBlock FontWeight="Bold" Text="{Binding HourData.Title, RelativeSource={RelativeSource TemplatedParent}}"/>

                                    <StackPanel Grid.Row="1">
                                        <StackPanel.Resources>
                                            <Style TargetType="th_wpf:TimeProgress">
                                                <Setter Property="Foreground" Value="{StaticResource Foreground_Normal}"/>
                                                <Setter Property="BarHeight" Value="10"/>
                                                <Setter Property="Margin" Value="0,5"/>
                                            </Style>
                                        </StackPanel.Resources>

                                        <th_wpf:TimeProgress Text="Production" 
                                             Value="{Binding HourData.ProductionSeconds, RelativeSource={RelativeSource TemplatedParent}}"
                                                             Maximum="{Binding HourData.TotalSeconds, RelativeSource={RelativeSource TemplatedParent}}"
                                             BarBrush="{StaticResource StatusGreen}" BarBackgroundBrush="{StaticResource StatusGreen_Hover}"/>

                                        <th_wpf:TimeProgress Text="Idle" Value="{Binding HourData.IdleSeconds, RelativeSource={RelativeSource TemplatedParent}}"
                                                             Maximum="{Binding HourData.TotalSeconds, RelativeSource={RelativeSource TemplatedParent}}"
                                             BarBrush="{StaticResource StatusYellow}" BarBackgroundBrush="{StaticResource StatusYellow_Hover}"/>

                                        <th_wpf:TimeProgress Text="Alert" Value="{Binding HourData.AlertSeconds, RelativeSource={RelativeSource TemplatedParent}}"
                                                             Maximum="{Binding HourData.TotalSeconds, RelativeSource={RelativeSource TemplatedParent}}"
                                             BarBrush="{StaticResource StatusRed}" BarBackgroundBrush="{StaticResource StatusRed_Hover}"/>

                                    </StackPanel>

                                </Grid>
                            </Rectangle.ToolTip>
                            <Rectangle.Style>
                                <Style TargetType="Rectangle">
                                    <Setter Property="Fill" Value="#FFF"/>
                                    <Style.Triggers>

                                        <DataTrigger Binding="{Binding HourData.Status, RelativeSource={RelativeSource TemplatedParent}}" Value="2">
                                            <Setter Property="Fill" Value="{StaticResource StatusGreen}"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding HourData.Status, RelativeSource={RelativeSource TemplatedParent}}" Value="1">
                                            <Setter Property="Fill" Value="{StaticResource StatusYellow}"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding HourData.Status, RelativeSource={RelativeSource TemplatedParent}}" Value="0">
                                            <Setter Property="Fill" Value="{StaticResource StatusRed}"/>
                                        </DataTrigger>

                                        <MultiDataTrigger>
                                            <MultiDataTrigger.Conditions>
                                                <Condition Binding="{Binding HourData.Status, RelativeSource={RelativeSource TemplatedParent}}" Value="2"/>
                                                <Condition Binding="{Binding IsMouseOver, RelativeSource={RelativeSource Self}}" Value="true"/>
                                            </MultiDataTrigger.Conditions>
                                            <Setter Property="Fill" Value="{StaticResource StatusGreen_Light}"/>
                                        </MultiDataTrigger>
                                        <MultiDataTrigger>
                                            <MultiDataTrigger.Conditions>
                                                <Condition Binding="{Binding HourData.Status, RelativeSource={RelativeSource TemplatedParent}}" Value="1"/>
                                                <Condition Binding="{Binding IsMouseOver, RelativeSource={RelativeSource Self}}" Value="true"/>
                                            </MultiDataTrigger.Conditions>
                                            <Setter Property="Fill" Value="{StaticResource StatusYellow_Light}"/>
                                        </MultiDataTrigger>
                                        <MultiDataTrigger>
                                            <MultiDataTrigger.Conditions>
                                                <Condition Binding="{Binding HourData.Status, RelativeSource={RelativeSource TemplatedParent}}" Value="0"/>
                                                <Condition Binding="{Binding IsMouseOver, RelativeSource={RelativeSource Self}}" Value="true"/>
                                            </MultiDataTrigger.Conditions>
                                            <Setter Property="Fill" Value="{StaticResource StatusRed_Light}"/>
                                        </MultiDataTrigger>

                                    </Style.Triggers>
                                </Style>
                            </Rectangle.Style>
                        </Rectangle>

                        <!--Separator-->
                        <Rectangle Canvas.Top="14" Height="1" Width="{TemplateBinding ActualWidth}" Fill="#AAA"/>

                        <!--Segment Indicators-->
                        <UniformGrid Canvas.Top="15" Height="50" Width="{TemplateBinding ActualWidth}" Columns="12">
                            <UniformGrid.Resources>
                                <Style TargetType="Rectangle">
                                    <Setter Property="Fill" Value="#FFF"/>
                                    <Setter Property="ToolTip" Value="{Binding Title}"/>
                                    <Style.Triggers>

                                        <DataTrigger Binding="{Binding Status}" Value="2">
                                            <Setter Property="Fill" Value="{StaticResource StatusGreen}"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Status}" Value="1">
                                            <Setter Property="Fill" Value="{StaticResource StatusYellow}"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Status}" Value="0">
                                            <Setter Property="Fill" Value="{StaticResource StatusRed}"/>
                                        </DataTrigger>

                                        <MultiDataTrigger>
                                            <MultiDataTrigger.Conditions>
                                                <Condition Binding="{Binding Status}" Value="2"/>
                                                <Condition Binding="{Binding IsMouseOver, RelativeSource={RelativeSource Self}}" Value="true"/>
                                            </MultiDataTrigger.Conditions>
                                            <Setter Property="Fill" Value="{StaticResource StatusGreen_Light}"/>
                                        </MultiDataTrigger>
                                        <MultiDataTrigger>
                                            <MultiDataTrigger.Conditions>
                                                <Condition Binding="{Binding Status}" Value="1"/>
                                                <Condition Binding="{Binding IsMouseOver, RelativeSource={RelativeSource Self}}" Value="true"/>
                                            </MultiDataTrigger.Conditions>
                                            <Setter Property="Fill" Value="{StaticResource StatusYellow_Light}"/>
                                        </MultiDataTrigger>
                                        <MultiDataTrigger>
                                            <MultiDataTrigger.Conditions>
                                                <Condition Binding="{Binding Status}" Value="0"/>
                                                <Condition Binding="{Binding IsMouseOver, RelativeSource={RelativeSource Self}}" Value="true"/>
                                            </MultiDataTrigger.Conditions>
                                            <Setter Property="Fill" Value="{StaticResource StatusRed_Light}"/>
                                        </MultiDataTrigger>

                                    </Style.Triggers>
                                </Style>
                            </UniformGrid.Resources>

                            <Rectangle Grid.Column="0" DataContext="{Binding HourData.SegmentDatas[0], RelativeSource={RelativeSource TemplatedParent}}"/>
                            <Rectangle Grid.Column="1" DataContext="{Binding HourData.SegmentDatas[1], RelativeSource={RelativeSource TemplatedParent}}"/>
                            <Rectangle Grid.Column="2" DataContext="{Binding HourData.SegmentDatas[2], RelativeSource={RelativeSource TemplatedParent}}"/>
                            <Rectangle Grid.Column="3" DataContext="{Binding HourData.SegmentDatas[3], RelativeSource={RelativeSource TemplatedParent}}"/>
                            <Rectangle Grid.Column="4" DataContext="{Binding HourData.SegmentDatas[4], RelativeSource={RelativeSource TemplatedParent}}"/>
                            <Rectangle Grid.Column="5" DataContext="{Binding HourData.SegmentDatas[5], RelativeSource={RelativeSource TemplatedParent}}"/>
                            <Rectangle Grid.Column="6" DataContext="{Binding HourData.SegmentDatas[6], RelativeSource={RelativeSource TemplatedParent}}"/>
                            <Rectangle Grid.Column="7" DataContext="{Binding HourData.SegmentDatas[7], RelativeSource={RelativeSource TemplatedParent}}"/>
                            <Rectangle Grid.Column="8" DataContext="{Binding HourData.SegmentDatas[8], RelativeSource={RelativeSource TemplatedParent}}"/>
                            <Rectangle Grid.Column="9" DataContext="{Binding HourData.SegmentDatas[9], RelativeSource={RelativeSource TemplatedParent}}"/>
                            <Rectangle Grid.Column="10" DataContext="{Binding HourData.SegmentDatas[10], RelativeSource={RelativeSource TemplatedParent}}"/>
                            <Rectangle Grid.Column="11" DataContext="{Binding HourData.SegmentDatas[11], RelativeSource={RelativeSource TemplatedParent}}"/>

                        </UniformGrid>

                    </Canvas>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>


</ResourceDictionary>
