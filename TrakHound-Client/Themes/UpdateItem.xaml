<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:TrakHound_Client.Pages.Options.Updates">


    <!--<Style TargetType="{x:Type local:UpdateItem}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:UpdateItem}">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">

                        <StackPanel>

                            <CheckBox IsChecked="{TemplateBinding IsSelected}"/>

                            <TextBlock Text="{TemplateBinding ApplicationTitle}"/>

                            <TextBlock Text="{TemplateBinding ApplicationSubtitle}"/>

                            <TextBlock Text="{TemplateBinding AvailableVersion}"/>

                        </StackPanel>
                        
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>-->
    
</ResourceDictionary>
