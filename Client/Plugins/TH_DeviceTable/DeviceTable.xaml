<UserControl x:Class="TH_DeviceTable.DeviceTable"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             
             xmlns:components="clr-namespace:TH_DeviceTable.Components"
             
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="800">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/TH_Styles;component/All.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Background="#003263">
    <!--<Grid Background="#151313">-->
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <Grid Grid.Row="2">
            <Grid.Background>
                <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                    <GradientStop Color="Transparent" Offset="0"/>
                    <!--<GradientStop Color="#40000000" Offset="0.8"/>-->
                    <GradientStop Color="#40000000" Offset="1"/>
                </LinearGradientBrush>
            </Grid.Background>
        </Grid>
        
        <!--Column Headers-->
        <Grid Height="40">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300"/>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <Grid.Background>
                <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                    <GradientStop Color="#20ffffff" Offset="0"/>
                    <GradientStop Color="#20ffffff" Offset="0.3"/>
                    <GradientStop Color="Transparent" Offset="1"/>
                </LinearGradientBrush>
            </Grid.Background>

            <!--Seperator-->
            <Rectangle Grid.Column="1" Width="1" Fill="{StaticResource Shade_Light}"/>

            <ItemsControl Grid.Column="2" ItemsSource="{Binding ColumnHeaders}" Margin="0,0,0,0">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>

            <!--Gloss Effect-->
            <components:GlossOverlay Grid.ColumnSpan="3"/>

        </Grid>

        <!--Seperator-->
        <!--<Rectangle Grid.Row="1" Height="1" VerticalAlignment="Bottom" Fill="{StaticResource Shade_Light}"/>-->

        <!--Rows-->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!--Row Headers-->
                <ItemsControl ItemsSource="{Binding RowHeaders}"/>

                <!--Seperator-->
                <!--<Rectangle Grid.Column="1" Width="1" Fill="{StaticResource Shade_Light}"/>-->

                <!--Rows-->
                <ItemsControl Grid.Column="2" ItemsSource="{Binding Rows}"/>

            </Grid>

        </ScrollViewer>
        
    </Grid>

</UserControl>
