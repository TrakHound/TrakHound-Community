<UserControl x:Class="TH_DeviceTable.DeviceTable"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             
             xmlns:local="clr-namespace:TH_DeviceTable"
             
             xmlns:th_wpf="clr-namespace:TH_WPF;assembly=TH_WPF"
             
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="600">

    <UserControl.Resources>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="MinHeight" Value="30"/>
            <Setter Property="Background" Value="{StaticResource Accent_Normal}"/>
            <Setter Property="Foreground" Value="{StaticResource Foreground_Opposite_Normal}"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type DataGridColumnHeader}">
                        <Border Name="bd"
                            Background="{TemplateBinding Background}"
                            Margin="0,0,0,0"
                            BorderBrush="{Binding VerticalGridLineBrush, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type DataGrid}}}" 
                            BorderThickness="0,0,1,0"    
                            >
                            <Grid>
                                <!--Content-->
                                <Border Padding="10,0">
                                    <ContentPresenter  VerticalAlignment="Center" HorizontalAlignment="Center" />
                                </Border>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter Property="Background" Value="{StaticResource Accent_Hover}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <ContextMenu x:Key="DataGridRowContextMenu">
            <local:DataGridMenuItem Header="Open Device Page" DataObject="{Binding}" Click="DataGridMenuItem_Click"/>
        </ContextMenu>
        
        <Style TargetType="DataGridRow" BasedOn="{StaticResource {x:Type DataGridRow}}">
            <Setter Property="BorderBrush" Value="{StaticResource LightBorder}" />
            <Setter Property="BorderThickness" Value="0,1,0,0" />
            <Setter Property="ContextMenu" Value="{StaticResource DataGridRowContextMenu}"/>
            <Setter Property="MinHeight" Value="50"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding Connected}" Value="false">
                    <Setter Property="Foreground" Value="{StaticResource Shade_Normal}"/>
                </DataTrigger>
                <Trigger Property="IsMouseOver" Value="true">
                    <Setter Property="Background" Value="{StaticResource Accent_Hover}"/>
                    <Setter Property="Foreground" Value="{StaticResource Foreground_Normal}"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="true">
                    <Setter Property="Background" Value="{StaticResource Accent_Hover}"/>
                    <Setter Property="Foreground" Value="{StaticResource Foreground_Normal}"/>
                </Trigger>
            </Style.Triggers>
        </Style>


        <Style x:Key="CellNoPadding" TargetType="DataGridCell">
            <Setter Property="BorderThickness" Value="0,0,0,0"/>
            <Setter Property="MinHeight" Value="35"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type DataGridCell}">
                        <Border Background="{TemplateBinding Background}"
                                Margin="0"
                                Padding="0"
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="0"
                                >
                            <ContentPresenter VerticalAlignment="Stretch" HorizontalAlignment="Stretch" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsFocused" Value="false">
                    <Setter Property="Foreground" Value="{StaticResource Foreground_Normal}"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="true">
                    <Setter Property="Background" Value="{Binding Background, RelativeSource={RelativeSource Self}}"/>
                    <Setter Property="BorderBrush" Value="Transparent"/>
                    <Setter Property="Foreground" Value="{StaticResource Foreground_Normal}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <SolidColorBrush x:Key="High" Color="#FF19B419"/>
        <SolidColorBrush x:Key="Medium" Color="Orange"/>
        <SolidColorBrush x:Key="Low" Color="Red"/>

        <Style TargetType="{x:Type th_wpf:MeterDisplay}">
            <Setter Property="Foreground" Value="{StaticResource Foreground_Normal}" />
            <Setter Property="ActiveLevelBrush" Value="{StaticResource Accent_Normal}"/>
            <Setter Property="InactiveLevelBrush" Value="{StaticResource Accent_Hover}"/>
            <Setter Property="FontSize" Value="24pt"/>
            <Setter Property="Maximum" Value="1"/>
            <Setter Property="ValueFormat" Value="P1"/>
            <Setter Property="Width" Value="auto"/>
            <Setter Property="Padding" Value="5"/>
        </Style>

    </UserControl.Resources>

    <Grid>

        <DataGrid x:Name="Devices_DG" Grid.Row="0" ItemsSource="{Binding DeviceInfos}"
                  AutoGenerateColumns="False"
                  BorderBrush="Transparent"
                  HeadersVisibility="Column"
                  >

            <DataGrid.Columns>

                <!--Description Columns-->
                <DataGridTextColumn Header="Device ID" Binding="{Binding Description.Device_ID}"/>
                <DataGridTextColumn Header="Description" Binding="{Binding Description.Description}"/>

                <!--OEE-->
                <DataGridTemplateColumn Header="OEE" CellStyle="{StaticResource CellNoPadding}">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <th_wpf:MeterDisplay Value="{Binding Oee}" >
                                <th_wpf:MeterDisplay.Style>
                                    <Style TargetType="th_wpf:MeterDisplay" BasedOn="{StaticResource {x:Type th_wpf:MeterDisplay}}">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding OeeStatus}" Value="0">
                                                <Setter Property="ActiveLevelBrush" Value="White"/>
                                                <Setter Property="InactiveLevelBrush" Value="#88ffffff"/>
                                                <Setter Property="Foreground" Value="White" />
                                                <Setter Property="Background" Value="{StaticResource Low}"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding OeeStatus}" Value="1">
                                                <Setter Property="ActiveLevelBrush" Value="{StaticResource Medium}"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding OeeStatus}" Value="2">
                                                <Setter Property="ActiveLevelBrush" Value="{StaticResource High}"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </th_wpf:MeterDisplay.Style>
                            </th_wpf:MeterDisplay>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <!--Availability-->
                <DataGridTemplateColumn Header="Availability" CellStyle="{StaticResource CellNoPadding}">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <th_wpf:MeterDisplay Value="{Binding Availability}" >
                                <th_wpf:MeterDisplay.Style>
                                    <Style TargetType="th_wpf:MeterDisplay" BasedOn="{StaticResource {x:Type th_wpf:MeterDisplay}}">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding AvailabilityStatus}" Value="0">
                                                <Setter Property="ActiveLevelBrush" Value="White"/>
                                                <Setter Property="InactiveLevelBrush" Value="#88ffffff"/>
                                                <Setter Property="Foreground" Value="White" />
                                                <Setter Property="Background" Value="{StaticResource Low}"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding AvailabilityStatus}" Value="1">
                                                <Setter Property="ActiveLevelBrush" Value="{StaticResource Medium}"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding AvailabilityStatus}" Value="2">
                                                <Setter Property="ActiveLevelBrush" Value="{StaticResource High}"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </th_wpf:MeterDisplay.Style>
                            </th_wpf:MeterDisplay>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <!--Performance-->
                <DataGridTemplateColumn Header="Performance" CellStyle="{StaticResource CellNoPadding}">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <th_wpf:MeterDisplay Value="{Binding Performance}" >
                                <th_wpf:MeterDisplay.Style>
                                    <Style TargetType="th_wpf:MeterDisplay" BasedOn="{StaticResource {x:Type th_wpf:MeterDisplay}}">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding PerformanceStatus}" Value="0">
                                                <Setter Property="ActiveLevelBrush" Value="White"/>
                                                <Setter Property="InactiveLevelBrush" Value="#88ffffff"/>
                                                <Setter Property="Foreground" Value="White" />
                                                <Setter Property="Background" Value="{StaticResource Low}"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding PerformanceStatus}" Value="1">
                                                <Setter Property="ActiveLevelBrush" Value="{StaticResource Medium}"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding PerformanceStatus}" Value="2">
                                                <Setter Property="ActiveLevelBrush" Value="{StaticResource High}"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </th_wpf:MeterDisplay.Style>
                            </th_wpf:MeterDisplay>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <!--Production Status-->
                <!--<DataGridTextColumn Header="Production Status" Binding="{Binding ProductionStatus}"/>-->
                <DataGridTemplateColumn Header="Production Status" CellStyle="{StaticResource CellNoPadding}">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <th_wpf:TimeProgress Text="{Binding ProductionStatus}" Value="34" Maximum="100">
                                <th_wpf:TimeProgress.Style>
                                    <Style TargetType="th_wpf:TimeProgress">
                                        <Setter Property="Foreground" Value="{StaticResource Foreground_Normal}"/>
                                        <Setter Property="BarBrush" Value="{StaticResource Accent_Normal}"/>
                                        <Setter Property="BarBackgroundBrush" Value="{StaticResource Accent_Hover}"/>
                                        <Setter Property="BarHeight" Value="10"/>
                                    </Style>
                                </th_wpf:TimeProgress.Style>
                            </th_wpf:TimeProgress>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <!--CNC Controller Status Variables-->
                <DataGridTextColumn Header="Emergency Stop" Binding="{Binding EmergencyStop}">
                    <DataGridTextColumn.CellStyle>
                        <Style TargetType="DataGridCell">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type DataGridCell}">
                                        <Border
                                            Margin="0"
                                            BorderBrush="{TemplateBinding BorderBrush}" 
                                            BorderThickness="0"
                                            >
                                            <Border.Style>
                                                <Style TargetType="Border">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Text, ElementName=txt}" Value="ARMED">
                                                            <Setter Property="Background" Value="#FF19B419"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Text, ElementName=txt}" Value="TRIGGERED">
                                                            <Setter Property="Background" Value="Red"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>
                                            <Grid>

                                                <TextBlock x:Name="txt" Margin="10,0" Text="{Binding EmergencyStop}" VerticalAlignment="Center" HorizontalAlignment="Center">
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Setter Property="Foreground" Value="White"/>
                                                            <Setter Property="FontWeight" Value="Bold"/>
                                                            <Setter Property="FontSize" Value="12pt"/>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>

                                                <!--Gloss Overlay-->
                                                <Border IsHitTestVisible="False">

                                                    <Grid IsHitTestVisible="False">
                                                        <Grid.RowDefinitions>
                                                            <RowDefinition Height="4.5*"/>
                                                            <RowDefinition Height="5.5*"/>
                                                        </Grid.RowDefinitions>

                                                        <Border IsHitTestVisible="False">
                                                            <Border.Background>
                                                                <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                                                                    <GradientStop Color="#10ffffff" Offset="0"/>
                                                                    <GradientStop Color="#30ffffff" Offset="1"/>
                                                                </LinearGradientBrush>
                                                            </Border.Background>
                                                        </Border>

                                                        <Border Grid.Row="1" IsHitTestVisible="False">
                                                            <Border.Background>
                                                                <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                                                                    <GradientStop Color="#10000000" Offset="0"/>
                                                                    <GradientStop Color="#00ffffff" Offset="1"/>
                                                                </LinearGradientBrush>
                                                            </Border.Background>
                                                        </Border>

                                                        <Border Grid.RowSpan="2" IsHitTestVisible="False">
                                                            <Border.Background>
                                                                <LinearGradientBrush EndPoint="1,0.5" StartPoint="0,0.5">
                                                                    <GradientStop Color="#10000000" Offset="0"/>
                                                                    <GradientStop Color="#10ffffff" Offset="0.5"/>
                                                                    <GradientStop Color="#10000000" Offset="1"/>
                                                                </LinearGradientBrush>
                                                            </Border.Background>
                                                        </Border>

                                                    </Grid>

                                                </Border>

                                            </Grid>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </DataGridTextColumn.CellStyle>
                </DataGridTextColumn>
                
                <DataGridTextColumn Header="Controller Mode" Binding="{Binding ControllerMode}">
                    <DataGridTextColumn.CellStyle>
                        <Style TargetType="DataGridCell">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type DataGridCell}">
                                        <Border
                                            Margin="0"
                                            BorderBrush="{TemplateBinding BorderBrush}" 
                                            BorderThickness="0"
                                            >
                                            <Border.Style>
                                                <Style TargetType="Border">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Text, ElementName=txt}" Value="AUTOMATIC">
                                                            <Setter Property="Background" Value="#FF19B419"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>
                                            <Grid>

                                                <TextBlock x:Name="txt" Margin="10,0" Text="{Binding ControllerMode}" VerticalAlignment="Center" HorizontalAlignment="Center">
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Setter Property="Foreground" Value="{StaticResource Foreground_Normal}"/>
                                                            <Setter Property="FontWeight" Value="Bold"/>
                                                            <Setter Property="FontSize" Value="12pt"/>
                                                            <Style.Triggers>
                                                                <Trigger Property="Text" Value="AUTOMATIC">
                                                                    <Setter Property="Foreground" Value="White"/>
                                                                </Trigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>

                                                <!--Gloss Overlay-->
                                                <Border IsHitTestVisible="False">

                                                    <Grid IsHitTestVisible="False">
                                                        <Grid.RowDefinitions>
                                                            <RowDefinition Height="4.5*"/>
                                                            <RowDefinition Height="5.5*"/>
                                                        </Grid.RowDefinitions>

                                                        <Border IsHitTestVisible="False">
                                                            <Border.Background>
                                                                <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                                                                    <GradientStop Color="#10ffffff" Offset="0"/>
                                                                    <GradientStop Color="#30ffffff" Offset="1"/>
                                                                </LinearGradientBrush>
                                                            </Border.Background>
                                                        </Border>

                                                        <Border Grid.Row="1" IsHitTestVisible="False">
                                                            <Border.Background>
                                                                <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                                                                    <GradientStop Color="#10000000" Offset="0"/>
                                                                    <GradientStop Color="#00ffffff" Offset="1"/>
                                                                </LinearGradientBrush>
                                                            </Border.Background>
                                                        </Border>

                                                        <Border Grid.RowSpan="2" IsHitTestVisible="False">
                                                            <Border.Background>
                                                                <LinearGradientBrush EndPoint="1,0.5" StartPoint="0,0.5">
                                                                    <GradientStop Color="#10000000" Offset="0"/>
                                                                    <GradientStop Color="#10ffffff" Offset="0.5"/>
                                                                    <GradientStop Color="#10000000" Offset="1"/>
                                                                </LinearGradientBrush>
                                                            </Border.Background>
                                                        </Border>

                                                    </Grid>

                                                </Border>

                                            </Grid>
                                           
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </DataGridTextColumn.CellStyle>
                </DataGridTextColumn>
                
                <DataGridTextColumn Header="Execution Mode" Binding="{Binding ExecutionMode}">
                    <DataGridTextColumn.CellStyle>
                        <Style TargetType="DataGridCell">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type DataGridCell}">
                                        <Border
                                            Margin="0"
                                            BorderBrush="{TemplateBinding BorderBrush}" 
                                            BorderThickness="0"
                                            >
                                            <Border.Style>
                                                <Style TargetType="Border">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Text, ElementName=txt}" Value="ACTIVE">
                                                            <Setter Property="Background" Value="#FF19B419"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Text, ElementName=txt}" Value="STOPPED">
                                                            <Setter Property="Background" Value="Red"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>

                                            <Grid>

                                                <TextBlock x:Name="txt" Margin="10,0" Text="{Binding ExecutionMode}" VerticalAlignment="Center" HorizontalAlignment="Center">
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Setter Property="Foreground" Value="{StaticResource Foreground_Normal}"/>
                                                            <Setter Property="FontWeight" Value="Bold"/>
                                                            <Setter Property="FontSize" Value="12pt"/>
                                                            <Style.Triggers>
                                                                <Trigger Property="Text" Value="ACTIVE">
                                                                    <Setter Property="Foreground" Value="White"/>
                                                                </Trigger>
                                                                <Trigger Property="Text" Value="STOPPED">
                                                                    <Setter Property="Foreground" Value="White"/>
                                                                </Trigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>

                                                <!--Gloss Overlay-->
                                                <Border IsHitTestVisible="False">

                                                    <Grid IsHitTestVisible="False">
                                                        <Grid.RowDefinitions>
                                                            <RowDefinition Height="4.5*"/>
                                                            <RowDefinition Height="5.5*"/>
                                                        </Grid.RowDefinitions>

                                                        <Border IsHitTestVisible="False">
                                                            <Border.Background>
                                                                <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                                                                    <GradientStop Color="#10ffffff" Offset="0"/>
                                                                    <GradientStop Color="#30ffffff" Offset="1"/>
                                                                </LinearGradientBrush>
                                                            </Border.Background>
                                                        </Border>

                                                        <Border Grid.Row="1" IsHitTestVisible="False">
                                                            <Border.Background>
                                                                <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                                                                    <GradientStop Color="#10000000" Offset="0"/>
                                                                    <GradientStop Color="#00ffffff" Offset="1"/>
                                                                </LinearGradientBrush>
                                                            </Border.Background>
                                                        </Border>

                                                        <Border Grid.RowSpan="2" IsHitTestVisible="False">
                                                            <Border.Background>
                                                                <LinearGradientBrush EndPoint="1,0.5" StartPoint="0,0.5">
                                                                    <GradientStop Color="#10000000" Offset="0"/>
                                                                    <GradientStop Color="#10ffffff" Offset="0.5"/>
                                                                    <GradientStop Color="#10000000" Offset="1"/>
                                                                </LinearGradientBrush>
                                                            </Border.Background>
                                                        </Border>

                                                    </Grid>

                                                </Border>

                                            </Grid>
                                            
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </DataGridTextColumn.CellStyle>
                </DataGridTextColumn>

            </DataGrid.Columns>

        </DataGrid>

    </Grid>
    
</UserControl>
