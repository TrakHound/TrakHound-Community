<UserControl x:Class="TH_History.History"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             
             xmlns:th_wpf="clr-namespace:TH_WPF;assembly=TH_WPF"
             
             xmlns:categorybargraph="clr-namespace:TH_DataCenter.Controls.Graphs.Bar.Category"
             
             xmlns:controls="clr-namespace:TH_History.Controls"
             
             mc:Ignorable="d" 
             d:DesignHeight="300"
             d:DesignWidth="800"
             >

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/TH_Styles;component/All.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto" MinWidth="150"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <!--Device List-->
        <Border BorderBrush="{StaticResource Accent_Dark}" BorderThickness="0,0,1,0" Background="{StaticResource Accent_Normal}">

            <ItemsControl ItemsSource="{Binding DeviceList}">
                <ItemsControl.Resources>
                    <Style TargetType="th_wpf:ListButton">
                        <Setter Property="Foreground" Value="{StaticResource Foreground_Opposite_Normal}"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter Property="Background" Value="#33ffffff"/>
                            </Trigger>
                        </Style.Triggers>
                        
                    </Style>
                </ItemsControl.Resources>
            </ItemsControl>

        </Border>
        
        <Grid Grid.Column="1" Background="{StaticResource Window_Background}">
            
            <StackPanel>

                <StackPanel>

                    <TextBlock Text="Device Description"/>
                    <TextBlock Text="Device Manufacturer"/>
                    <TextBlock Text="Device Model"/>
                    <TextBlock Text="Device Serial"/>
                    <TextBlock Text="Device ID"/>

                </StackPanel>

                <!--Shift Display-->
                <controls:ShiftDisplay x:Name="shiftDisplay"/>
                
                <!--<StackPanel Orientation="Horizontal">

                    --><!--Day-->
                    <!--<controls:ShiftInfo x:Name="Day_ShiftInfo" />-->

                    <!--Week--><!--
                    <controls:ShiftInfo x:Name="Week_ShiftInfo" />

                    --><!--Month--><!--
                    <controls:ShiftInfo x:Name="Month_ShiftInfo" />--><!--


                </StackPanel>-->
                
                
                
                
            </StackPanel>
            
            
            
            
            
            
            
            
        </Grid>


        <!--<categorybargraph:Graph x:Name="graph1" Grid.Column="1" />-->

    </Grid>
    
</UserControl>
