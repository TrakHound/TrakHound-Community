<UserControl x:Class="TH_History.Controls.Calendar.Calendar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             
             MaxWidth="500"
             >
    
    <StackPanel Margin="30,20">
        
        <StackPanel Orientation="Horizontal" Margin="0,10">

            <!--Month Name-->
            <TextBlock Text="{Binding MonthName}" FontSize="20pt" Foreground="#333" Margin="0" VerticalAlignment="Center"/>

            <!--Year-->
            <TextBlock Text="{Binding Year}" FontSize="16pt" Foreground="#666666" Margin="20,0" VerticalAlignment="Center"/>

        </StackPanel>

        <UniformGrid x:Name="DayOfWeek_UGRID" Columns="7" />

        <UniformGrid x:Name="Days_UGRID" Columns="7" Background="#aa0080ff"/>
        
    </StackPanel>
    
</UserControl>
