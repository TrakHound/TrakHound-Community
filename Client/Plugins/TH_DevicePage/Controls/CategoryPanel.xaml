<UserControl x:Class="TH_DevicePage.Controls.CategoryPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TH_DevicePage.Controls"
             
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             >

    <Border x:Name="root">
        <Border.Style>
            <Style TargetType="Border">
                <Setter Property="BorderBrush" Value="#33ffffff"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="CornerRadius" Value="2"/>
                <Setter Property="Margin" Value="5"/>
                <Setter Property="Background" Value="{Binding Background}"/>
            </Style>
        </Border.Style>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!--Title Bar-->
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="auto"/>
                </Grid.ColumnDefinitions>

                <!--Image-->
                <Grid Margin="5,0,10,0" Height="20" Width="{Binding ActualHeight, RelativeSource={RelativeSource Self}}">
                    <Grid.Resources>
                        <ImageBrush x:Key="IMG" Stretch="Uniform" ImageSource="{Binding Image}" />
                    </Grid.Resources>
                    <Grid.Style>
                        <Style TargetType="Grid">
                            <Setter Property="OpacityMask" Value="{StaticResource IMG}" />
                            <Setter Property="Background" Value="{StaticResource Foreground_Opposite_Normal}"/>
                        </Style>
                    </Grid.Style>
                </Grid>
                
                <!--Title-->
                <TextBlock Grid.Column="1" Text="{Binding Title}" Foreground="{StaticResource Foreground_Opposite_Normal}" FontSize="12pt" Margin="0,5"/>

                <!--Id-->
                <TextBlock Grid.Column="2" Text="{Binding Id}" Foreground="{StaticResource Foreground_Opposite_Normal}" FontSize="12pt" Margin="10,5"/>

            </Grid>
            
            <!--Separator-->
            <Rectangle Grid.Row="1" Fill="#33ffffff" Height="1"/>
            
            <!--Content-->
            <ContentControl Grid.Row="2" Content="{Binding Content}" Margin="10"/>

        </Grid>

    </Border>

</UserControl>
