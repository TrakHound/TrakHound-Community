<UserControl x:Class="TH_DevicePage.Controls.AxisStatusPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TH_DevicePage.Controls"
             
             xmlns:th_wpf="clr-namespace:TH_WPF;assembly=TH_WPF"
             
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             >
    
    <Grid>

        <StackPanel>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                </Grid.RowDefinitions>
                <Grid.Resources>
                    <Style TargetType="local:TextDisplay">
                        <Setter Property="Width" Value="250"/>
                        <Setter Property="Height" Value="40"/>
                        <Setter Property="Margin" Value="10,5"/>
                        <Setter Property="Foreground" Value="{StaticResource Foreground_Opposite_Normal}"/>
                    </Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="VerticalAlignment" Value="Center"/>
                        <Setter Property="HorizontalAlignment" Value="Center"/>
                        <Setter Property="Margin" Value="10,5,5,5"/>
                        <Setter Property="Foreground" Value="{StaticResource Foreground_Opposite_Normal}"/>
                    </Style>
                </Grid.Resources>

                <!--Axis Name-->
                <TextBlock Grid.Column="0" Grid.Row="1" Text="Programmed Position"/>
                <local:TextDisplay Grid.Column="1" Grid.Row="1" Text="6000 RPM"/>

                <!--Programmed Position-->
                <TextBlock Grid.Column="0" Grid.Row="1" Text="Programmed Position"/>
                <local:TextDisplay Grid.Column="1" Grid.Row="1" Text="6000 RPM"/>

                <!--Actual Speed-->
                <TextBlock Grid.Column="0" Grid.Row="3" Text="Actual Speed"/>
                <local:TextDisplay Grid.Column="1" Grid.Row="3" Text="5998.9 RPM"/>

                <!--Spindle Load-->
                <TextBlock Grid.Column="0" Grid.Row="4" Text="Spindle Load"/>
                <Grid Grid.Column="1" Grid.Row="4" Margin="10,5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="auto"/>
                    </Grid.ColumnDefinitions>

                    <th_wpf:ProgressBar Grid.Column="0" Grid.Row="4" Value="0.41" Maximum="1" Foreground="#fff" Background="#33ffffff" Height="30" VerticalAlignment="Center"/>

                    <TextBlock Grid.Column="1" Text="41%" FontSize="16pt" Margin="10,0"/>

                </Grid>

            </Grid>

            <local:OverrideStatus Title="Spindle Override" Margin="10"/>

        </StackPanel>

    </Grid>
    
</UserControl>
