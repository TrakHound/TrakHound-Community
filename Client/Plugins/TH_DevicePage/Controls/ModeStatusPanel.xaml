<UserControl x:Class="TH_DevicePage.Controls.ModeStatusPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TH_DevicePage.Controls"
             
             mc:Ignorable="d" 
             d:DesignHeight="300"
             d:DesignWidth="300"
             >

    <Border x:Name="root">
        <Border.Style>
            <Style TargetType="Border">
                <Setter Property="BorderBrush" Value="#33ffffff"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="CornerRadius" Value="2"/>
            </Style>
        </Border.Style>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!--Title-->
            <TextBlock Text="{Binding Title}" Foreground="{StaticResource Foreground_Opposite_Normal}" TextWrapping="Wrap" TextAlignment="Center" HorizontalAlignment="Center" FontSize="10pt" Margin="10,5"/>

            <!--Separator-->
            <Rectangle Grid.Row="1" Fill="#33ffffff" Height="1"/>

            <local:ImageDisplay Grid.Row="2" Image="{Binding Image}">
                <local:ImageDisplay.Style>
                    <Style TargetType="local:ImageDisplay">
                        <Setter Property="Height" Value="50"/>
                        <Setter Property="Width" Value="50"/>
                        <Setter Property="Margin" Value="10"/>
                        <Setter Property="Foreground" Value="{StaticResource Foreground_Opposite_Normal}"/>
                    </Style>
                </local:ImageDisplay.Style>
            </local:ImageDisplay>

        </Grid>

    </Border>
    
</UserControl>
