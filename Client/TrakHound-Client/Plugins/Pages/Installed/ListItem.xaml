<!--Copyright (c) 2015 Feenux LLC, All Rights Reserved.-->

<!--This file is subject to the terms and conditions defined in-->
<!--file 'LICENSE.txt', which is part of this source code package.-->

<UserControl x:Class="TrakHound_Client.Plugins.Pages.Installed.ListItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
                          
             x:Name="listitem"
             >

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/TH_Styles;component/All.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Border Background="#00000000" BorderBrush="{StaticResource VeryLightText}" BorderThickness="0.5" ToolTipService.ShowDuration="10000" ToolTipService.BetweenShowDelay="500" ToolTipService.InitialShowDelay="500">
        <Border.ToolTip>
            <ToolTip DataContext="{Binding Path=PlacementTarget.DataContext, RelativeSource={RelativeSource Self}}">
                <Grid Margin="5">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="auto"/>
                    </Grid.RowDefinitions>

                    <!--PlugIn Info-->
                    <Grid Grid.Row="0" Margin="0,10,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!--Plugin Image-->
                        <Rectangle Height="30" Width="30" Fill="{StaticResource DarkText}" VerticalAlignment="Top" Margin="0,0,10,0">
                            <Rectangle.Resources>
                                <ImageBrush x:Key="IMG" ImageSource="{Binding PlugIn_Image}" Stretch="Uniform" />
                            </Rectangle.Resources>

                            <Rectangle.Style>
                                <Style TargetType="Rectangle">
                                    <Setter Property="OpacityMask" Value="{StaticResource IMG}" />
                                </Style>
                            </Rectangle.Style>
                        </Rectangle>
                        
                        <!--Plugin Title and Version-->
                        <Grid Grid.Column="1">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="auto"/>
                                <RowDefinition Height="auto"/>
                            </Grid.RowDefinitions>

                            <!--Plugin Title-->
                            <TextBlock Grid.Row="0" Text="{Binding PlugIn_Title}" FontSize="20" Foreground="{StaticResource DarkText}"/>

                            <!--Plugin Version-->
                            <TextBlock Grid.Row="1" Text="{Binding PlugIn_Version}" FontSize="12" Foreground="{StaticResource LightText}"/>

                        </Grid>

                    </Grid>
                    
                    <!--PlugIn Description-->
                    <TextBlock Grid.Row="1" Margin="0,10" MaxWidth="200" TextWrapping="Wrap" Text="{Binding PlugIn_Description}" FontSize="10" Foreground="{StaticResource LightText}"/>
                    
                    <!--Author Info-->
                    <Grid Grid.Row="2" Margin="0,20,0,5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!--Author Image-->
                        <Image MaxHeight="50" MaxWidth="100" Source="{Binding Author_Image}" Margin="0,0,10,0"/>
                        
                        <Grid Grid.Column="1">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <!--Author Name-->
                            <TextBlock Grid.Row="0" Text="{Binding Author_Name}" FontSize="16" Foreground="{StaticResource DarkText}"/>

                            <!--Author Text-->
                            <TextBlock Grid.Row="1" Text="{Binding Author_Text}" FontSize="10" Foreground="{StaticResource LightText}"/>

                        </Grid>
                        
                    </Grid>
                    
                </Grid>
                
            </ToolTip>
        </Border.ToolTip>
        
        <Border.ContextMenu>
            <ContextMenu FontSize="14">
                <MenuItem Header="About" Click="MenuItem_About_Click">
                    <MenuItem.Icon>
                        <Rectangle Height="20" Width="20" Fill="{StaticResource DWBlue}" HorizontalAlignment="Center" VerticalAlignment="Center">
                            <Rectangle.Resources>
                                <ImageBrush x:Key="IMG" ImageSource="/TrakHound-Client;component/Resources/About_01.png" Stretch="Uniform" />
                            </Rectangle.Resources>

                            <Rectangle.Style>
                                <Style TargetType="Rectangle">
                                    <Setter Property="OpacityMask" Value="{StaticResource IMG}" />
                                </Style>
                            </Rectangle.Style>
                        </Rectangle>
                    </MenuItem.Icon>
                </MenuItem>
            </ContextMenu>
        </Border.ContextMenu>

        <Grid MinHeight="50" MinWidth="300">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>

            <Grid Margin="20,10,10,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!--Image-->
                <Rectangle Height="25" Width="25" Fill="{StaticResource DarkText}" VerticalAlignment="Top" Margin="0,5,0,0">
                    <Rectangle.Resources>
                        <ImageBrush x:Key="IMG" ImageSource="{Binding PlugIn_Image, ElementName=listitem}" Stretch="Uniform" />
                    </Rectangle.Resources>

                    <Rectangle.Style>
                        <Style TargetType="Rectangle">
                            <Setter Property="OpacityMask" Value="{StaticResource IMG}" />
                        </Style>
                    </Rectangle.Style>
                </Rectangle>

                <!--Title and Description-->
                <Grid Grid.Column="1" Margin="10,5">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="auto"/>
                    </Grid.RowDefinitions>

                    <!--Title-->
                    <TextBlock Text="{Binding PlugIn_Title, ElementName=listitem}" Foreground="{StaticResource DarkText}" FontSize="20" VerticalAlignment="Center"/>

                    <!--Description-->
                    <TextBlock Grid.Row="1" Text="{Binding PlugIn_Description, ElementName=listitem}" Foreground="{StaticResource LightText}" FontSize="12" MaxWidth="400" TextWrapping="Wrap" HorizontalAlignment="Left" />

                </Grid>     

            </Grid>
            
            <!--Enabled Text and Checkbox-->
            <Grid Grid.Column="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock Grid.Column="0" Text="Enabled" VerticalAlignment="Center" Margin="5,0" FontSize="16" Foreground="{StaticResource LightText}" />

                <CheckBox Grid.Column="1" Style="{StaticResource Android_CheckBoxStyle}" IsChecked="{Binding PlugIn_Enabled, ElementName=listitem}" Checked="CheckBox_Checked" Unchecked="CheckBox_Unchecked" Height="40"/>

            </Grid>

        </Grid>

    </Border>
    
</UserControl>
